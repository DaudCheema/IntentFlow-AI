# IntentFlow-AI
An asynchronous FastAPI agent that resolves query ambiguity, manages conversation history, and dynamically triggers concurrent web scraping before synthesizing multi-source responses using Groq LPUs.
